<div>
  {{#if tasks}}
    <table class='table table-bordered table-hover table-dark'>
      <thead>
        <tr>
          <th>N.</th>
          <th>Task</th>
          <th>Description</th>
          <th>Operations</th>
        </tr>
      </thead>

      <tbody>
        {{#each tasks}}
          <tr>
            {{!-- <td>{{@index}}</td> --}}
            <td>{{displayIndex}}</td>
            <td>{{title}}</td>
            <td>{{description}}</td>
            <td>

              {{#if done}}
                <a href='/toggleDone/{{_id}}' class='btn btn-dark'>Undone</a>
              {{else}}
                <a href='/toggleDone/{{_id}}' class='btn btn-success'>Done</a>
              {{/if}}

              <a href='/edit/{{_id}}' class='btn btn-info'>Edit</a>
              <a href='/del/{{_id}}' class='btn btn-danger'>Delete</a>
            </td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  {{else}}
    <div class='text-center'>
      <h1>No Tasks</h1>
      <img src='/icons/icons8w11-o120.png' />
    </div>
  {{/if}}
</div>